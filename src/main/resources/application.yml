spring:
  main:
    allow-circular-references: true
  application:
    name: wealthguard_backend
  profiles:
    active: dev

springdoc:
  swagger-ui:
    path: /api-docs.html
    operationsSorter: method