spring:
  datasource:
    url: jdbc:mysql://localhost:3306/wealthguard
    username: root
    password: admin
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

debug: true